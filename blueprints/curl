name="curl"
source="https://github.com/curl/curl/releases/download/curl-8_20_0/curl-8.20.0.tar.gz"

needs=()

    steps=("sh configure --with-openssl --without-libpsl --prefix=\$PREFIX"
            "make -j$NPROC install")
