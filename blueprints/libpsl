name="libpls"
source="https://github.com/rockdaboot/libpsl/releases/download/0.21.5/libpsl-0.21.5.tar.gz"

needs=()

    steps=("sh configure --prefix=$PREFIX" "make -j$NPROC" "make check" "make install")
